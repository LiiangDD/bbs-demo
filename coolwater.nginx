server {
    listen 80;

    location /images {
        alias /var/www/coolwater/images;
    }

    location /static {
        alias /var/www/coolwater/static;
    }

    location / {
        proxy_pass http://localhost:2000;
    }
}
